<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/news_item_card"
    android:layout_width="@dimen/news_item_card_size"
    android:layout_height="@dimen/news_item_card_size">
    <ImageView
        android:id="@+id/news_item_image"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="fitXY" />
    <TextView
        android:id="@+id/news_item_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_alignParentBottom="true"
        android:padding="@dimen/news_item_text_padding"
        android:background="#80000000"
        android:textColor="#FFFFFF"
        android:textSize="@dimen/news_item_text_size"
        android:text="placeholder" />
    <ImageButton
        android:id="@+id/news_item_button"
        android:src="@drawable/ic_action_save"
        android:background="?android:attr/selectableItemBackground"
        android:scaleType="fitXY"
        android:layout_width="@dimen/news_item_save_button_size"
        android:layout_height="@dimen/news_item_save_button_size"
        android:layout_alignParentTop="true"
        android:layout_alignParentRight="true"
        android:layout_marginEnd="@dimen/news_item_save_button_padding"
        android:layout_marginTop="@dimen/news_item_save_button_padding" />
</RelativeLayout>